// AUTO-GENERATED by scripts/build-model-phone-image-index.py
// This placeholder is replaced from the local verified phone catalog before deployment.

const MODEL_PHONE_IMAGE_INDEX = Object.freeze({});

function normalize(value) {
  return String(value || "")
    .toLowerCase()
    .replace(/[^a-z0-9]+/g, " ")
    .replace(/\s+/g, " ")
    .trim();
}

const BRAND_ALIASES = {
  iphone: "apple",
  mi: "xiaomi",
  redmi: "xiaomi",
  poco: "xiaomi",
  pixel: "google",
  moto: "motorola",
  iqoo: "vivo",
  "one plus": "oneplus",
  "real me": "realme",
};

function normalizeBrand(value) {
  const n = normalize(value);
  return BRAND_ALIASES[n] || n;
}

function normalizeModel(value) {
  const parts = normalize(value).split(" ").filter(Boolean);
  const removable = new Set([
    "apple","google","xiaomi","samsung","motorola","oneplus","oppo","realme",
    "vivo","iqoo","huawei","honor","nokia","sony","tecno","infinix","itel",
    "lg","htc","zte","asus"
  ]);
  while (parts.length && removable.has(parts[0])) parts.shift();
  return parts.join(" ");
}

export function getMappedPhoneImageByIdentity(brand, model) {
  const b = normalizeBrand(brand);
  const m = normalizeModel(model);
  if (!m) return "";
  return MODEL_PHONE_IMAGE_INDEX[`${b}|${m}`] || "";
}
